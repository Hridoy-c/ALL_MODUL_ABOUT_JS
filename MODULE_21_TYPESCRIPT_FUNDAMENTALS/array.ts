let number : number[] = [1, 2, 3, 4, 5]; // Initializes an array of numbers

number.push(6); // Adds 6 to the end of the array

let userName : string[] = ["Alice", "Bob", "Charlie"]; // Initializes an array of strings

userName.pop(); // Removes the last element ("Charlie") from the array